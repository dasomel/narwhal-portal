# Narwhal Portal 사건 기록 (Lessons Log)

> 자매 레포 `narwhal`의
> [`docs/common/lessons-log.md`](https://github.com/dasomel/narwhal/blob/main/docs/common/lessons-log.md)와
> 같은 규칙을 따른다. 날짜별 사건 서술은 여기에, 여러 사건에 걸쳐 일반화되는 규칙만
> `CLAUDE.md` / `AGENTS.md`에 남긴다. 고치다 만든 실수도 같은 비중으로 기록한다.
>
> 항목 추가 형식: `| YYYY-MM-DD | 사건 설명 | 조치 |`
>
> 새 행을 추가하기 전에 증상으로 먼저 grep한다. 근접 항목이 있으면 그 행을 날카롭게 다듬고,
> 두 번째 행을 덧붙이지 않는다. 기록할 것은 결론이 아니라 **판별자** — 닮은 원인들과 이 원인을
> 어떻게 구분하는지, 그리고 그럴듯하지만 틀린 처방이 무엇인지.

## Mistakes Log (Compounding Engineering)

### Documentation / Release Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-09-06 | (고치다 만든 실수) 워커 레인 여러 개가 같은 워킹트리를 쓰는 동안 #28 커밋을 만들면서 `git diff docs/lessons-log.md`로 #28 행 하나만 있는 걸 확인한 뒤 `git add docs/lessons-log.md`를 했다. 그 두 명령 사이에 #27 레인이 자기 행을 같은 파일에 써서, #27의 lessons-log 행이 #28 커밋(`2ba8fc5`)에 실려 갔다 — 루트 `AGENTS.md`가 경고하는 `e82d8e1`과 정확히 같은 모양이고, 파일 단위 스테이징도 막지 못했다 | 이력은 고치지 않고 #27 커밋 메시지에 그 행이 `2ba8fc5`에 있다고 적었다. 판별자: **동시 레인이 있으면 "diff 확인 → add"는 원자적이지 않다.** 여러 레인이 공유하는 파일(lessons-log 같은 append-only 로그)은 `git add -p`로 검토한 hunk만 스테이징하거나, 그 파일을 쓰는 작업을 직렬화하라. 파일 단위 스테이징은 *다른 파일*의 혼입만 막는다 |
| 2026-08-09 | `release.yml`의 CHANGELOG 섹션 추출이 **모든 버전에서 조용히 빈 결과**를 냈다. 원인은 `awk -v p="^## \\[${v}\\]"` — `awk -v`는 값에 든 백슬래시 이스케이프를 소비하므로 `\[`가 `[`로 줄어들어 정규식이 `^## [1.0.16]`, 즉 문자 클래스가 된다. 이 클래스는 `## ` 다음의 한 글자를 요구하므로 실제 제목과 절대 일치하지 않는다. 자매 레포 narwhal의 원본은 같은 정규식을 **프로그램 문자열에 직접 보간**해서(`awk "/^## \[${VERSION}\]/{...}"`) 이 경로를 타지 않았고, 그래서 "그대로 옮기면 된다"는 가정이 틀렸다. awk는 경고조차 내지 않았고 종료 코드도 0이었다 | 제목 일치를 정규식이 아니라 **접두사 비교**로 바꿨다: `awk -v target="## [${version}]" 'index($0, target) == 1'`. 이스케이프 문제가 사라질 뿐 아니라 버전의 `.`이 자동으로 리터럴이 되어 `1.0.1`이 `1.0.15` 제목에 걸리는 별개의 함정까지 같이 막힌다. 판별자: `awk -v`로 넘긴 값이 정규식이면 항상 의심하라 — 실패가 "매치 없음"으로 나타나기 때문에 데이터가 없는 것과 구분되지 않는다. 그리고 이 버그를 잡은 것은 코드 리뷰가 아니라 **커밋 전에 7개 태그 전부로 추출을 돌려본 것**이다. 존재하는 태그가 하나라도 빈 결과면 로직이 틀린 것이고, 존재하지 않는 태그가 빈 결과여야 가드가 맞는 것 — 두 방향을 다 돌려야 판별된다 |
| 2026-08-09 | 커밋 제목만 주고 CHANGELOG를 작성시킨 워커가 **읽지 않은 구현을 그럴듯하게 지어냈다.** v1.0.2의 "k8s Events informer"를 "native `client-go` event informer"로 서술했는데, 이 레포는 TypeScript이고 `client-go`는 한 글자도 등장하지 않는다. 실제 구현은 `src/lib/live-k8s-informer.ts`의 `fetch`로 여는 core/v1 Events watch(`?watch=1`, `resourceVersion` + 백오프)다. 같은 산출물에서 Kaniko Job 매니페스트 파일명도 `narwhal-portal-k8s.yaml`로 지어냈다(실제 `deploy/kaniko-build-job.yaml`). 두 오류 모두 커밋 제목에는 없는 정보를 "채워 넣은" 자리에서 발생했고, 문장 자체는 완벽하게 자연스러웠다 | 산출물의 **모든 백틱 식별자를 기계적으로 grep**해 존재를 확인한 뒤에만 커밋한다 — 경로·env 이름·쿠키 이름·라우트·이미지 참조·컨트롤 ID 전부. 판별자: 오류는 어색한 문장이 아니라 *가장 구체적으로 들리는* 문장에 숨는다("native client-go"는 주변 어떤 문장보다 전문적으로 읽힌다). 커밋 제목은 무엇이 바뀌었는지만 말하고 어떻게 바뀌었는지는 말하지 않으므로, 구현을 서술하는 문장은 반드시 코드에서 다시 확인해야 한다. 부수 효과로, 실제 코드를 읽자 더 나은 서술이 나왔다 — `docker-publish.yml` 주석에는 v1.0.16 이미지가 QEMU arm64 빌드 2h38m 타임아웃으로 아예 발행되지 못했다는 사실이 적혀 있었고, 워커가 쓴 일반론("QEMU 대신 네이티브 러너 사용")보다 이쪽이 릴리스 노트로서 훨씬 유용하다 |

### Licensing / Redistribution Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-18 | 공개 레포에 **OSS 라이선스 고지가 한 줄도 없었다.** `LICENSE` 없음, `package.json`에 `license` 필드 없음, README에 라이선스 절 없음, `NOTICE`/`THIRD-PARTY-NOTICES` 없음 — GitHub API도 `license: null`을 반환했다. 더 무거운 쪽은 **배포 이미지**였다. `next build`의 파일 트레이싱은 서버가 로드하는 것만 남기므로 `LICENSE` 파일을 걷어낸다: 1.0.17 빌드 기준 `node_modules` 631개 → `.next/standalone` **3개**(그마저 `fetch-blob`과 `grpc-js` 내부 proto). MIT/BSD/ISC는 "all copies or substantial portions"에 저작권·허가 고지를 요구하고 Apache-2.0 §4(d)는 NOTICE 전파를 요구하는데, 이미지가 바로 그 copy다. 레포에 의존성을 vendoring 하지 않는다는 사실이 "재배포하지 않는다"는 뜻으로 오독되기 쉽다 — 재배포는 이미지에서 일어난다 | `LICENSE`(Apache-2.0, narwhal과 동일) + `NOTICE`(사람이 읽는 요약) + `scripts/generate-third-party-notices.mjs`가 생성하는 `THIRD-PARTY-NOTICES.md`(프로덕션 659패키지, 라이선스 원문 포함)를 추가하고, Dockerfile runner 스테이지가 셋 다 이미지에 `COPY` 한다. 생성물이므로 `license-and-sbom.yml`이 재생성 후 diff로 신선도를 강제하고, `--strict`가 PERMISSIVE에도 ACCEPTED에도 없는 라이선스에서 CI를 세운다. 판별자: **"레포에 없다"와 "배포물에 없다"는 다른 질문이다.** 라이선스 준수는 소스 트리가 아니라 *아티팩트*를 기준으로 세야 하고, 그 확인 명령은 `find .next/standalone -type f -iname 'LICENSE*' \| wc -l`처럼 산출물을 직접 세는 것이어야 한다. `.dockerignore`에 `*.md`가 있어 `!THIRD-PARTY-NOTICES.md` 재포함이 필요했던 것도 같은 이유로 놓치기 쉽다 |
| 2026-08-18 | (고치다 만든 실수) libvips(LGPL-3.0-or-later)를 이미지에서 빼려고 `images: { unoptimized: true }`를 넣고 **끝난 줄 알았다.** 근거는 타당해 보였다 — `src` 전체에 `next/image` import가 0건이니 optimizer는 쓰이지 않는다. 그러나 재빌드 후 `.next/standalone/node_modules/@img`에 `sharp-libvips-*`가 **그대로 남아 있었다.** Next는 optimizer 사용 여부와 무관하게 sharp를 트레이싱에 포함한다. 설정 한 줄로 산출물이 바뀔 것이라 믿고 확인하지 않았다면 "LGPL 제거함"이라고 잘못 보고할 뻔했다 | `outputFileTracingExcludes`로 `sharp`/`@img`를 명시적으로 제외한 뒤 재빌드해 실제 파일 0개를 확인하고, standalone 서버를 **기동해서** `/`가 307(인증 리다이렉트)로 응답하고 모듈 오류가 없음까지 확인했다. `build-check.yml`에 산출물과 이미지 양쪽에 대한 libvips 부재 단언을 추가했다. 판별자: 설정 플래그의 이름(`unoptimized`)이 산출물 구성까지 바꿔줄 것이라는 추정은 **트레이싱과 런타임 분기가 별개**라는 사실 앞에서 깨진다. 빌드 산출물에 관한 주장은 산출물을 `find`로 세서만 확정하고, 제외한 모듈이 실제로 필요 없었는지는 서버를 띄워 라우트 응답으로 확인한다 |

### Authorization / Scoping Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-20 | 읽기 API 네 곳이 **인증만 하고 인가는 하지 않았다.** `/api/namespaces`는 guest에게도 전체 네임스페이스를, `/api/catalog`는 전체 ArgoCD 앱을, `/api/events`는 전체 배포 이력과 알림을 반환했고, `/api/k8s/pods`·`/api/k8s/resource`는 호출자가 보낸 `namespace`를 검증 없이 그대로 읽었다(`iam`·`database` 지정 가능). 판별에 쓸 `getVisibilityScope`는 **이미 존재했고 `/api/my-apps`와 SSE는 쓰고 있었다** — 없어서 못 한 게 아니라, 같은 질문을 라우트마다 인라인 식으로 각자 판단하다 보니 그중 몇이 아예 판단하지 않게 된 것이다. `/api/namespaces` POST는 `body.team`을 소유권 라벨로 그대로 신뢰해, developer가 자기 팀이 아닌 팀 소유로 네임스페이스를 만들고 그 팀 매핑을 통해 조작할 수 있었다 | 판정을 `src/lib/role-filter.ts`로 모았다 — `appMatchesScope`·`alertMatchesScope`·`projectMatchesScope`·`scopeFingerprint` — 그리고 `/api/my-apps`의 인라인 사본 2개와 private `hashGroups`를 제거해 구현이 하나만 남게 했다. `role-filter.test.ts`(20건)가 harness-rules 워크플로에서 돈다. 판별자: **"헬퍼가 없다"와 "헬퍼를 안 쓴다"는 다른 문제이고, 후자가 더 흔하다.** 인가 누락을 찾을 때는 스코프 함수를 grep해서 *쓰는* 곳이 아니라, 같은 데이터를 반환하면서 *안 쓰는* 곳을 찾아야 한다 — `getArgoApps()` 호출자 목록과 `getVisibilityScope()` 호출자 목록의 차집합이 정확히 결함 목록이었다. **(2026-08-24 재발)** 이 차집합 기법을 최초 스윕 이후 다시 돌리지 않아서, `/api/catalog`가 고쳐진 뒤에 생긴/못 본 형제 라우트 4개(`catalog/[name]`, `scorecards`, `scorecards/[svc]`, `governance/scorecard`)가 전부 미스코프 상태로 남아 있었다 — `catalog/[name]`은 이름만 알면 팀 무관하게 전체 상세를 반환하는 직접 참조 우회였고, `governance/scorecard`는 아래(2026-08-20, 두 번째 행)와 **정확히 같은 모양**의 캐시 오염 버그(단일 리터럴 키 `"governance:scorecard"`)를 독립적으로 갖고 있었다. 판별자: **차집합 기법은 한 번 돌리고 끝나는 감사가 아니라 새 라우트가 `getArgoApps()`/`getAlerts()`를 호출할 때마다 재실행해야 하는 불변식이다** — 원본 이슈(#31)가 닫히지 않고 "관련 엔드포인트도 일관되게 스코프하라"를 요구사항으로 남겨둔 것도 같은 이유. **(2026-08-25 재발, 이번엔 코드가 아니라 이슈 코멘트에서 놓침)** #31/#33/#14의 라우트 레벨 negative 테스트를 추가하려고 8개 라우트(`catalog`, `catalog/[name]`, `scorecards`, `scorecards/[svc]`, `governance/scorecard`, `k8s/pods`, `k8s/resource`, `events`)를 읽는 김에 `/api/argocd`와 `/api/governance/dora`도 확인했다 — 둘 다 **여전히 `@/lib/scope`를 아예 import하지 않는다.** 그런데 이 둘은 #31 이슈 코멘트(2026-08-19, "Please treat #31 as covering this endpoint")에 **명시적으로 스코프 대상**이라고 적혀 있었다. 두 번의 수정 커밋(1e48371, b36356a) 모두 건드리지 않고 지나갔다 — 두 커밋의 트리아지 코멘트가 `getArgoApps()` 호출자 차집합을 코드에서는 다시 돌렸지만, **이슈 코멘트에 적힌 "이것도 스코프하라"는 텍스트 목록은 그 차집합의 입력에 없었다.** `governance/dora`는 추가로 `governance/scorecard`가 이미 두 번 고친 것과 같은 모양의 캐시 오염(`"governance:dora:v2"` 단일 리터럴 키)까지 그대로 갖고 있다. 판별자: **차집합 기법은 코드에서 호출자를 세는 것이지, 사람이 이슈에 적어둔 "이것도"를 세지 않는다.** 코드 기반 스윕과 이슈/코멘트 텍스트 기반 체크리스트는 서로 다른 누락을 잡으므로 둘 다 필요하다 — 이번 것은 테스트 추가 작업(portal-route-negtests) 중 우연히 발견했고, 고치지는 않았다(범위 밖) — #31에 새 코멘트로 남김. **(2026-09-07 재발, 이번엔 코드가 아니라 테스트가 빠짐)** #14를 감사하려고 `src/app/api/events/route.ts`를 다시 읽자 `appVisible`/`alertVisible`/`scope.fingerprint` 캐시 키가 이미 1e48371에서 들어가 있었다 — 이슈가 요구하는 보안 수정 자체는 이미 배포돼 있었다. 그런데 `route.test.ts`에는 4건뿐이었고 전부 팀 간 교차 테넌트 케이스와 캐시 키 분리만 증명했다 — 인수조건 중 `cluster-admin behavior remains unchanged`와, 스코프를 세울 수 없는 비관리자의 기본 거부(`namespace-less/system events require explicit visibility policy`에 대응)는 어느 테스트도 실행한 적이 없었다. `route.test.ts`에 admin 전체 뷰(두 팀 배포를 모두 봄), 스코프 없는 비관리자의 빈 타임라인, Alertmanager 네임스페이스 라벨 필터링 3건을 추가했다(298 passed, 기존 295건 + 3건). 판별자: **코드 감사에서 "이미 고쳐져 있다"는 결론과 "이슈를 닫아도 된다"는 결론은 다르다.** 인수조건 각 항목이 회귀 테스트로 개별 매핑돼 있는지 확인해야 하며, 매핑이 없으면 코드가 지금 맞더라도 다음 리팩터가 관리자 예외나 기본-거부 분기를 조용히 깨도 잡아낼 수 없다 |
| 2026-09-03 | `/api/scorecards?owner=X`가 서비스 목록에는 owner 필터를 적용했지만, `tierCounts`는 그 전에 계산했다. 따라서 목록은 X의 서비스만 보이는데 gold/silver/bronze 집계는 같은 호출자가 볼 수 있는 **모든** 소유자를 포함했다. `evaluateAll(ownerFilter, tierFilter)`의 owner 인자는 실제 필터링에는 쓰이지 않고 캐시 키만 분리해, 책임 위치도 그럴듯하게 잘못 암시했다 | 소유자·가시성 필터를 `serviceMap`을 가진 라우트에서 평가 결과에 한 번 적용한 뒤, 그 하나의 집합으로 목록·총계·티어 집계를 모두 만들었다. `evaluateAll`은 소유자를 받지 않고 티어만 필터한다. 판별자: **필터가 있는 응답의 각 파생값이 같은 행 집합에서 만들어지는지 확인하라.** 목록만 사후 필터링하고 집계는 이전 집합에서 계산하면 개별 항목 단언은 통과해도 두 결과가 같은 쿼리를 설명하지 못한다. 회귀 테스트는 서로 다른 소유자와 티어를 섞어, 반환 목록뿐 아니라 그 목록에서 재계산한 분포와 API `tierCounts`의 정확한 일치를 단언해야 한다 |
| 2026-09-06 | #27과 같은 모양의 결함이 `/api/cost`(#28)에도 있었다: `requireRole`로 인가만 하고 `getCost(scope)`가 반환하는 namespace/service 항목 전부를, 심지어 단일 `cluster` 합산까지도 스코프 없이 반환했다. 캐시 키(`cost:${scope}:all`)에도 스코프 지문이 없어 필터를 붙였다면 오염됐을 것 | `getCost`가 raw PromQL 행을 `namespaceVisible`로 걸러낸 뒤 namespace/service 목록과 `cluster` 합산을 **같은 필터링된 행 집합**에서 계산하게 하고, 캐시 키에 `effScope.fingerprint`를 넣었다(`src/lib/cost.ts:206`) |
| 2026-09-06 | #27의 남은 인수조건: `scorecard:all:${tierFilter}`/`scorecard:detail:${serviceId}` 평가 캐시 키가 규칙 ConfigMap 버전을 담지 않았다. 운영자가 ConfigMap을 갱신해도, 이미 계산돼 있던 평가는 자기 TTL(60s/300s)이 끝날 때까지 옛 규칙 그대로 계속 서빙됐다 — `loadRules()`가 그 문서를 별도로 300s TTL로 캐싱하고 있었는데도, 어떤 규칙 버전이 이 평가를 만들었는지를 평가 캐시 키가 전혀 몰랐다 | 두 캐시 조회 앞에 `loadRules()`를 먼저 호출해(그 자체가 캐시돼 있어 비용은 redis GET 한 번) 키를 `scorecard:all:${rules.version}:${tierFilter}`, `scorecard:detail:${rules.version}:${serviceId}`로 바꿨다. `evaluateAll`/`evaluateService`의 시그니처와 TTL은 그대로 뒀다. 판별자: **파생값의 캐시 키는 그 값에 영향을 줄 수 있는 모든 입력을 담아야 하고, 독립적인 자기 TTL로 따로 캐싱되는 입력도 그런 입력 중 하나다.** "입력 A가 이미 캐시돼 있다"는 사실이 "A가 바뀌어도 괜찮다"를 뜻하지 않는다 — A와 A에서 파생된 B가 서로 다른 TTL로 독립 캐싱되면, A가 바뀐 뒤에도 B의 키가 그대로인 한 B는 새 A를 절대 반영하지 못한다 |
| 2026-08-20 | (고치다 만든 실수) `/api/events`에 스코프 필터만 넣고 끝낼 뻔했다. 이 라우트는 **완성된 타임라인**을 `"events:timeline"`이라는 단일 리터럴 키에 캐싱한다. 필터만 추가했다면 먼저 캐시를 채운 사용자의 화면이 뒤따르는 모두에게 제공된다 — 필터가 없는 것보다 **좁고 훨씬 발견하기 어려운** 결함이며, 코드 리뷰에서는 필터가 추가된 diff만 보이므로 통과했을 가능성이 높다 | 키가 호출자 스코프 지문을 담게 했다(`events:timeline:${scopeFingerprint(...)}`). 판별자: **스코프 필터를 추가할 때는 반드시 그 결과가 캐싱되는지 같이 본다.** 구분 기준은 캐시가 *필터 이전*의 업스트림 응답을 담는지(안전, `/api/namespaces`·`getArgoApps`) *필터 이후*의 결과를 담는지(위험, 여기)다. 전자는 키를 그대로 둬도 되고 후자는 반드시 스코프를 키에 넣어야 한다 |
| 2026-08-30 | `federated-logout` 라우트의 리다이렉트 허용목록(`ALLOWED_REDIRECT_HOST_SUFFIXES`)이 **도메인 서픽스와 단일 호스트를 같은 배열에 섞고 같은 `endsWith()`로 검사**했다. `.narwhal.internal`은 앞에 점이 있어 서브도메인 경계가 강제되지만, `localhost`·`127.0.0.1`은 점이 없어 `endsWith()`가 **앵커되지 않은 부분 문자열 검사**가 됐다 — 호스트명이 그 글자로 "끝나기만" 하면 통과해, `maliciouslocalhost` 같은 호스트도 로그아웃 후 리다이렉트 대상으로 받아들여졌다(오픈 리다이렉트). 배경 자동 보안 리뷰가 잡아냈다 | 정확히 일치해야 하는 단일 호스트(`ALLOWED_REDIRECT_EXACT_HOSTS`)와 서브도메인을 허용해야 하는 진짜 서픽스(`ALLOWED_REDIRECT_HOST_SUFFIXES`, 계속 점 앵커 유지)를 분리했다. 회귀 테스트를 추가하다가 `127.0.0.1` 앞에 문자를 붙인 변형(`myevilhost127.0.0.1`)은 애초에 `new URL()`이 마지막 라벨이 순수 숫자면 전체를 IPv4로 엄격 검증해 생성 자체가 실패한다는 걸 확인했다(Codex 리뷰가 처음 제안한 테스트 문자열 `evil127.0.0.1.attacker.com`은 애초에 `127.0.0.1`로 끝나지 않아 버그를 증명하지 못했다). 판별자: **허용목록에 "정확히 이 호스트"와 "이 도메인의 서브도메인 전부"를 같은 컬렉션·같은 매칭 함수로 넣지 마라.** 후자만 `endsWith`가 안전하고(단, 반드시 선행 점 포함), 전자는 반드시 `===`/`.includes()`여야 한다 — 이 둘을 섞은 배열은 검토 시점에 "다 같은 화이트리스트"로 보여 앵커 누락이 숨는다 |
| 2026-08-20 | (고치다 만든 실수) 스코프 캐시 키용 지문을 만들면서 `/api/my-apps`의 private `hashGroups`를 그대로 올려 썼다. 원래 자리에서는 사용자별 캐시 키였지만, 공유 헬퍼로 올려 `/api/events`에서 **테넌트를 가르는 용도**로 쓰는 순간 요구 수준이 달라졌는데 구현은 그대로였다. 두 가지가 깨져 있었다. (1) teams를 `team:<name>`으로 **접두사 붙여 한 리스트에 평탄화**해서, `team:platform`이라는 *그룹*과 `platform` *팀* 소속이 동일한 값을 낸다 — 수정 전 양쪽 `91a0a4f0`으로 재현했다. 이름에 구분자가 들어가면 같은 모양의 문제가 전부 생긴다. (2) 32비트 rolling hash라 수만 개 스코프에서 birthday 충돌이 나고, 여기서의 충돌은 **다른 테넌트의 캐시된 타임라인**을 내주는 것이다. 같은 커밋에서 `/api/k8s/*`는 **검증하지 않은 namespace로 인가 판정**을 했다 — 스코프 패턴이 접두사 매칭이라 `platform-system/../iam`이 `platform-*`를 만족한다 | 지문을 `sha256(JSON.stringify([sortedGroups, sortedTeams]))`로 바꿔 접두사가 아니라 **구조로 분리**했다. k8s 라우트는 `assertK8sNamespace` → 스코프 판정 순서로 정정했다. 판별자: **공유 헬퍼로 승격하는 것은 리팩터링이 아니라 요구사항 변경이다.** 원래 자리에서 충분했던 강도가 새 호출자에게도 충분한지 따로 확인해야 하고, 특히 "사용자 구분"에서 "테넌트 격리"로 올라가면 해시 강도와 인코딩 모호성이 처음으로 문제가 된다. 그리고 하류에 검증이 있다는 사실은 인가 순서를 정당화하지 않는다 — 판정은 정규화된 값 위에서 내려야 하고, 하류 검증에 기대면 거부가 *우연*이 된다 |
| 2026-09-07 | #19: `src/lib/openbao.ts`의 `listSecrets()`가 필드 이름(`keys`)을 보여주려고 시크릿마다 `/v1/secret/data/<path>`를 읽었다 — 인벤토리 뷰 하나 때문에 KV **data-read** 권한 전체가 필요해진 것으로, 메타데이터만 노출하는 화면치고 과도한 최소권한 위반이었다. 더해서 목록 실패(권한 거부 등)와 개별 시크릿 메타데이터 실패 둘 다 조용히 `[]` 또는 0값 항목으로 뭉개져, 라우트가 반환하는 빈 배열이 "시크릿이 실제로 0개"인지 "메타데이터를 못 읽었다"인지 구분되지 않았다 | `/v1/secret/data/` 호출을 완전히 제거하고 `/v1/secret/metadata/<path>`만으로 version/createdTime/updatedTime을 채웠다 — KV v2 메타데이터는 필드 이름을 노출하지 않으므로 `keys`는 UI 타입에서 통째로 뺐다(`secrets-table.tsx`가 카운트 배지 대신 정적 "***" 배지 렌더). 목록·개별 메타데이터 조회 실패는 `SecretMetadataError`를 던지게 해 라우트가 `502 {error, degraded:true}`로 명시적으로 응답한다(기존의 조용한 `return []`을 대체). 판별자: **인벤토리/목록 뷰의 "실패"와 "진짜 빈 목록"이 같은 응답 모양(`[]`)을 공유하면, 그 둘을 사람도 코드도 구분할 수 없다** — 메타데이터 읽기가 실패할 수 있는 경로에서는 빈 배열이 아니라 별도 에러 타입/상태 코드로 실패를 표현해야 한다. 커버: `openbao.test.ts`(5건, mocked fetch로 `/data/` URL이 한 번도 요청되지 않음을 단언)·`route.test.ts`(5건, 401/403/degraded 경로) |
| 2026-09-07 | #63(health endpoints) 1차 구현에서 `/api/health/status`에 의존성 도달성 프로브(최대 8개 아웃바운드 호출)를 추가하면서 `/live`·`/ready`와 같은 인증 면제 취급을 그대로 물려받았다. `src/proxy.ts`의 matcher가 `/api` 전체를 세션 게이트에서 제외하므로, 이 라우트는 세션 없이 누구나 호출 가능했다 — 즉 미인증 호출 1건이 Keycloak/ArgoCD/Prometheus/Alertmanager/Gitea/OpenBao/Loki/Valkey로 8건의 아웃바운드 요청을 유발하는 **증폭 벡터**였고, 응답 자체가 **어느 백엔드 의존성이 죽었는지 지도를 그려주는 정찰 채널**이었다. "이 라우트는 시크릿을 노출하지 않는다"만 확인하고 "누가 호출할 수 있는가"는 확인하지 않은 것이 원인이다 | `requireRole("cluster-admin")` 게이트(`/api/cost` 등 다른 관리자 라우트와 동일한 `src/lib/auth.ts` 패턴)를 라우트 맨 앞에 추가해 미인증/비관리자 요청이 프로브를 트리거하기 전에 401/403(본문은 `{error: "Unauthorized"}`/`{error: "Forbidden"}`만, 세부 내역 없음)으로 반환한다. `/live`·`/ready`는 그대로 인증 면제로 뒀다 — `/ready`의 유일한 아웃바운드 호출은 Valkey ping 1건뿐이라 같은 증폭 문제가 없다. 회귀 테스트로 미인증/비관리자 호출 시 `fetch`가 전혀 불리지 않는 것과 관리자 세션에서는 전체 본문이 나오는 것을 각각 확인했다. 판별자: **"응답에 시크릿이 없다"와 "누가 이 응답을 받아도 되는가"는 다른 질문이다.** 아웃바운드 팬아웃이 있거나 인프라 토폴로지(어느 의존성이 살아있는지)를 드러내는 진단 라우트를 새로 만들 때는, 인증 면제 라우트(`/live`, `/ready`)의 패턴을 그대로 복사하기 전에 그 라우트가 실제로 인증 면제여도 안전한 이유(팬아웃 없음·정보 없음)를 먼저 각각 확인해야 한다 |

### Test Infrastructure Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-25 | vitest에 `"@" -> "./src"` alias 해석 설정이 **한 번도 존재한 적이 없었다.** `tsconfig.json`은 `paths: {"@/*": ["./src/*"]}`를 선언하지만 이건 tsc 타입체크와 Next.js 번들러에만 적용되고, vitest(Vite 기반)는 별도의 `resolve.alias` 설정이 필요하다 — 이 레포에는 `vitest.config.ts` 자체가 없었다. 그런데도 148개 기존 테스트는 전부 통과하고 있었다: `src/lib/*.ts`에서 실제로 vitest가 런타임에 실행하는 `@/types/*` 임포트가 우연히 **전부 `import type`**이었기 때문이다 — esbuild가 타입 전용 임포트를 모듈 그래프 해석 전에 지워버려서 alias 미해결이 드러나지 않았다. portal#21에서 `src/lib/role-filter.ts`/`scope.ts`/`operation-context.ts`가 `DEFAULT_CLUSTER_ID`(실제 값, 상수)를 `@/types/cluster`에서 임포트하면서 처음으로 실제(non-type) `@/...` 임포트가 vitest 실행 경로를 탔고, 즉시 `ERR_MODULE_NOT_FOUND: Cannot find package '@/types/cluster'`로 6개 테스트 파일이 한꺼번에 깨졌다 | `vitest.config.ts`에 `resolve.alias: { "@": "./src" }`(tsconfig의 alias와 동일 매핑)를 추가했다. 판별자: **"이 alias가 테스트에서 동작한다"는 그 alias로 값을 임포트한 코드가 실제로 테스트에 물려 실행됐을 때만 검증된 것이다.** `import type`으로만 쓰인 alias는 아무것도 증명하지 않는다 — 새 `@/...` 값 임포트를 lib 모듈에 추가할 때는 관련 테스트를 돌려 alias가 실제로 해석되는지 먼저 확인한다 |
| 2026-08-26 | SSE stream(`/api/events/stream`) 통합 테스트에서 mutation 라우트 호출 후 이벤트 수신을 검증할 때, `live-stream.ts`는 Valkey 부재 시 degraded in-memory 모드로 잘 전환되었으나 `getEffectiveScope` 내부의 `getNamespaces()`(`@/lib/k8s-client`)가 실측 K8s API 커넥션 시도로 5초 타임아웃을 유발했다 | 테스트 파일에서 `@/lib/k8s-client`의 `getNamespaces`를 `vi.mock`으로 즉시 빈 배열/목록을 반환하도록 스텁 처리했다. 판별자: SSE stream 테스트 타임아웃의 원인은 live stream pub/sub 연결이 아니라, 스코프 인가 판정을 위해 불리는 k8s API fetch 타이머일 수 있다 |

### Privileged Execution Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-23 | `k8s-job-runner.ts`의 노드 튜닝 Job이 **매 실행마다** `apk add --no-cache util-linux`로 nsenter를 컨테이너 시작 시점에 네트워크에서 받아왔다 — privileged + hostPID + hostNetwork로 호스트에 nsenter하는 Job인데, 그 이미지 자체가 매번 미고정 패키지를 fetch했다. `TUNING_JOB_IMAGE`에 digest pin(`getTuningImage()`의 `DIGEST_IMAGE_RE`)이 이미 있었지만, 런타임에 이미지 밖에서 도구를 끌어오는 경로는 그 핀이 막지 못한다 — "이미지가 digest로 고정됐다"와 "그 이미지가 실행하는 모든 것이 고정됐다"는 다른 질문이다. `RunJobOptions.script`가 raw shell 문자열이라 호출부가 임의 셸을 주입할 수 있는 것도 같은 축의 문제였다 | nsenter를 빌드 타임에 굽는 `deploy/tuning-job.Dockerfile`을 추가하고 런타임 `apk add`를 제거했다. `RunJobOptions.script`를 `RunJobOptions.targets: ApplyTarget[]`로 바꿔 `buildJobScript`가 `tuning-commands.ts`의 allowlist에서만 스크립트를 구성하게 했다(호출부는 원시 셸을 더 이상 전달할 수 없음). Job pod은 RBAC grant가 전혀 없는 전용 `narwhal-tuning-job` ServiceAccount(`automountServiceAccountToken: false`)로 실행되고, 반대 방향(in-cluster→pod, pod→in-cluster) 모두 deny-all NetworkPolicy가 붙는다(narwhal 저장소, 커밋 6fd186e) — 단 hostNetwork pod에는 NetworkPolicy 적용이 CNI 구현에 좌우된다는 한계를 Dockerfile/YAML 주석에 명시했지 "고쳤다"고 주장하지 않았다. 판별자: **이미지 digest 핀은 이미지 안에 있는 것만 고정한다.** 컨테이너 시작 스크립트가 `apk add`/`curl`/`pip install` 등으로 이미지 밖에서 뭔가를 끌어오면, 그 시점부터는 별도의 공급망 표면이다 — 확인 방법은 Dockerfile/entrypoint에서 런타임에 실행되는 모든 패키지 관리자 호출을 grep하는 것 |

### Supply Chain Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-23 | (고치다 만든 실수) #66 공급망 강화로 pnpm `minimumReleaseAge: 10080`(7일)을 넣었더니 **Dependabot 업데이트가 전부 실패**했다 — 보안 업데이트 포함. `pnpm update <pkg> --lockfile-only`가 13개 패키지 전부에서 `ERR_PNPM_NO_MATURE_MATCHING_VERSION Version 1.2.5 (released 3 days ago) of rolldown does not meet the minimumReleaseAge constraint / This error happened while installing the dependencies of vite@8.2.1`로 죽었다. rolldown은 **이미 락파일에 있던**, 내가 직전 커밋에서 승인한 버전이다. `minimumReleaseAge`는 변경이 *도입하는* 버전이 아니라 **해상 시점의 그래프 전체**에 걸리므로, 락파일 안에 7일 안 된 패키지가 하나라도 있으면 그것이 성숙할 때까지 모든 락파일 조작이 막힌다. 활발한 레포에서는 그 조건이 거의 항상 참이다. 커밋 자체는 초록이었다 — `--frozen-lockfile` 설치는 해상을 하지 않아 영향이 없고, 내 워크플로 3개는 전부 통과했다. 실패한 것은 같은 SHA에서 돈 Dependabot 잡 하나뿐이었고, 그건 내 워크플로 목록에 없어서 하마터면 "무관한 실패"로 넘길 뻔했다 | 쿨링 창을 **락파일 델타**로 옮겼다: `pnpm-workspace.yaml`에서 `minimumReleaseAge`를 빼고(다시 넣으면 `check-supply-chain-policy.mjs`가 CI를 세운다), `scripts/check-lockfile-cooling.mjs`가 base 커밋 대비 새로 들어온 `name@version`만 골라 npm registry의 `time[version]`으로 7일을 강제한다. 그래프에 새로 들어오는 버전은 막고, 이미 들어와 있는 버전의 재해상은 막지 않는다. 원래 죽던 명령(`pnpm update js-yaml@4.3.1 --lockfile-only`)이 이제 `Done in 4.6s`로 통과하는 것을 재현 트리에서 확인했다. 그런데 이걸로도 안 끝났다 — 다음 Dependabot 실행은 `corepack pnpm update ... --config.minimumReleaseAge=10080`으로 **똑같이** 죽었다. `dependabot.yml`의 `cooldown: default-days: 7`을 Dependabot이 pnpm CLI 플래그로 번역해 넣기 때문이다(10080분 = 7일, 값이 정확히 일치). 즉 같은 그래프 전역 제약이 내가 지운 곳이 아니라 **두 번째 경로로 다시 들어왔고**, 첫 실행(8d83ca1)에는 그 플래그가 없어서 `--config` 문자열로 grep했다면 원인을 못 찾았을 것이다. `cooldown`도 빼고 `check-supply-chain-policy.mjs`가 재등장을 막는다. 판별자: **강화 제어를 넣을 때는 그 제어가 "새 위험"이 아니라 "모든 변경"에 걸리는지 확인한다.** 그리고 한 곳에서 지웠다고 사라졌다고 믿지 말 것 — 같은 제약을 주입하는 경로가 설정 파일 말고 **업스트림 도구의 번역**으로도 존재한다. 확인은 실패한 잡의 **실제 명령줄**을 읽는 것뿐이다. 세 번째 경로도 있었다 — `cooldown`을 지우자 Dependabot은 PR을 열기 시작했지만 이번엔 `frozenLockfile: true`가 `ERR_PNPM_OUTDATED_LOCKFILE  ... - @tanstack/react-query (lockfile: ^5.95.2, manifest: ^5.101.4)`로 막았다. 의존성 범프는 package.json을 고치고 락파일을 **의도적으로 다시 만드는** 작업인데, 이 설정은 그 작업까지 포함해 모든 install에 걸린다. 지우고, `check-supply-chain-policy.mjs`가 워크플로·Dockerfile의 install 줄마다 `--frozen-lockfile`을 요구하는 쪽(원래 있던 검사 3)만 남겼다 — 같은 보장을 런타임이 아니라 리뷰 시점에 준다. 즉 세 개의 강화 설정 중 **둘이 정상 유지보수를 막았고**, 공통점은 셋 다 '델타'가 아니라 '모든 install/해상'에 거는 스위치였다는 것이다. 구분 기준은 제어가 *델타*에 적용되는지 *상태 전체*에 적용되는지다 — 후자는 처음엔 조용하다가 정상 유지보수 경로를 전부 잠근다. 그리고 보안 강화가 **보안 업데이트를 막는** 형태로 실패하면 순손실이다. 확인 방법: 강화 커밋 직후 `gh run list`를 내 워크플로뿐 아니라 **전체**로 보고, `event: dynamic`(Dependabot) 실패를 무관한 잡으로 치부하지 않는다 |

### Configuration Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-09-06 | #60 fail-fast 요구사항을 처음 만족시킨 커밋(0d85572)은 `getK8sApiServer()`를 프로덕션에서 throw하도록 만들었지만, 같은 값을 읽는 레거시 `export const K8S_API_SERVER = process.env.K8S_API_SERVER \|\| ...`를 남겨뒀다. 이 상수는 **모듈 최상위**에서 평가되므로, 나머지 8개 파일(`k8s-client.ts`, `k8s-job-runner.ts`, `trivy.ts`, `compliance.ts`, `scorecard.ts`, `live-k8s-informer.ts`, `prometheus.ts`, `kubeconfig.ts`)이 그대로 `K8S_API_SERVER`를 import하는 한, 그 throw를 상수 쪽으로 그대로 옮기면 — 즉 상수를 `getK8sApiServer()` 호출로 바꾸기만 하면 — import 시점에 곧바로 실행되어 `next build`가 프로덕션 env 없이 라우트 모듈을 로드하는 순간 빌드 자체를 깨뜨린다. "fail-fast를 만들었다"와 "그 fail-fast가 안전하게 배선됐다"는 다른 질문이었다 | 각 파일에서 `K8S_API_SERVER` 상수 import를 `getK8sApiServer()` 함수 import로 바꾸고, 호출을 **함수/핸들러 본문 안, 요청 처리 시점**으로 옮겼다(모듈 최상위 `const`에 두지 않음) — `live-k8s-informer.ts`처럼 USE_BEARER 게이팅이 있는 곳은 게이팅 자체를 함수 인자로 apiServer를 받는 형태로 lazy화했다. 모든 임포터가 이관된 뒤에야 레거시 상수를 삭제했다. `NODE_ENV=production` + 두 env 모두 unset 상태에서 `next build`가 여전히 성공하는지, 그리고 별도로 호출 시점에 `Missing required production configuration` 에러가 실제로 throw되는지를 **둘 다** 확인해야 검증이 끝난 것이다 — 하나만 통과시키면 반대쪽이 깨진다. 판별자: **fail-fast 설정값은 호출 시점에 평가되어야 하고, import/모듈 로드 시점에 평가되면 `next build`가 그 설정 부재로 실패하는 대상이 되어버린다.** 모듈 최상위 `const X = fn()` 형태로 어떤 프로덕션 필수값을 감싸고 있다면, 그 값이 정말 "이 요청을 처리할 때만" 필요한지 확인하고 필요하면 함수 호출로 늦춘다. Phase 3(2026-09-06)에서 같은 모양(`process.env.X ?? "http://localhost:..."` 형태의 모듈 최상위 상수)이 `TEMPO_URL`, `PROMETHEUS_URL`(4개 파일), `APISIX_ADMIN_URL`, `ARGOCD_URL`, `OPENBAO_ADDR`, `ALERTMANAGER_URL` 등 9개 지점에서 추가로 발견되어 `getDependencyUrl()` 제네릭 접근자와 파일별 지연 호출 함수로 동일하게 고쳤다 |
